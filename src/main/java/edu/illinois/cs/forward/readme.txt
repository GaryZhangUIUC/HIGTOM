0. put out.txt from java project to this directory.

1. Get csv on each level.
	grep "^0" out.txt > level1
	grep "^1" out.txt > level2
	grep "^2" out.txt > level3 

2. Use the ipython notebook (map.ipynb) to analyze info:
	by changing the line below to visualize different leaves. 
		if each[2] == '0':

3. The run.py script is to see the clustered words.

4. I use python2
